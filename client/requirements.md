## Packages
embla-carousel-autoplay | Auto-playing hero carousel
framer-motion | Page transitions and scroll animations
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
The contact form uses a POST request to /api/contact. Missing endpoints will fail gracefully via react-query and display a toast notification.
No additional configurations are required.
